export default class Scene {
    constructor() {
    }

    initialize(gl) {
        console.fatal('initialize not implemented.');
    }

    update(gl, deltaTime) {
        console.fatal('update not implemented.');
    }
}
